---
import BaseLayout from './BaseLayout.astro';
import Footer from '../components/Footer.astro';
import ResponsiveImage from '../components/ResponsiveImage.astro';

interface Props {
  title: string;
  description?: string;
  eyebrow?: string;
  heroImage?: string;
  pageTitle?: string;
}

const { title, description, eyebrow, heroImage, pageTitle } = Astro.props;
const headTitle = pageTitle ?? `${title} — Self Esteem`;
---

<BaseLayout title={headTitle} description={description} image={heroImage}>
  <div class="page">
    <header class="page-nav">
      <a class="page-nav__brand" href="/"><span>Ji's</span> Homepage</a>
      <nav aria-label="Primary">
        <a href="/studio">About</a>
        <a href="/work">Work</a>
        <a href="/blog">Blog</a>
        <a href="/contact">Contact</a>
      </nav>
    </header>

    <main class="page-main">
      {
        heroImage && (
          <div class="page-hero">
            <ResponsiveImage src={heroImage} width={1600} height={900} alt="" loading="eager" />
          </div>
        )
      }

      <section class="page-head">
        {eyebrow && <div class="page-eyebrow">{eyebrow}</div>}
        <h1 class="page-title">{title}</h1>
        {description && <p class="page-deck">{description}</p>}
      </section>

      <div class="page-body">
        <slot />
      </div>
    </main>

    <Footer />
  </div>
</BaseLayout>
